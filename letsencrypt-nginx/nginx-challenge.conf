server {
    listen 80;
    listen [::]:80;
    server_name a36ede1f.ngrok.io;

    location ~ /.well-known/acme-challenge {
        allow all;
        root /usr/share/nginx/html;
    }

    root /usr/share/nginx/html;
}